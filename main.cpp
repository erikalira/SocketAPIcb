#include <iostream>
#include "Socket.h"

using namespace std;

//servidor: bind >> listen >> accept >> recv

//cliente: connect > send

int main()
{
    ServerSocket socket;

    socket.close();

    return 0;
}
